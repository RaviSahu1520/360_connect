<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ========= REQUIRED PERMISSIONS ========= -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Android 13+ new foreground service types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Internet for signaling / future features -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="360 Connect"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThreeSixZeroConnect">

        <!-- ================== MAIN ACTIVITY ================== -->
        <activity
            android:name="com.company.app.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ThreeSixZeroConnect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ================== CALL RECORDING SERVICE ================== -->
        <service
            android:name="com.company.app.platform.recording.CallRecordingService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone" />

        <!-- ================== ACCESSIBILITY CALL DETECTOR ================== -->
        <service
            android:name="com.company.app.platform.call.CallDetectionService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ================== NOTIFICATION LISTENER ================== -->
        <service
            android:name="com.company.app.platform.call.CallNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ================== VOIP (future, safe to keep) ================== -->
        <service
            android:name="com.company.app.platform.call.VoipSignalingService"
            android:exported="false" />

        <!-- ================== IN-CALL SERVICE (Android Telecom API) ================== -->
        <service
            android:name="com.company.app.presentation.call.InCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="false" />

    </application>
</manifest>
